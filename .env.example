# This project does not require any secrets to train or play locally.
# This file exists only to document optional integrations.

# Optional: Weights & Biases experiment tracking. Leave unset to disable.
WANDB_API_KEY=replace-with-your-real-key
WANDB_PROJECT=flappy-rl
